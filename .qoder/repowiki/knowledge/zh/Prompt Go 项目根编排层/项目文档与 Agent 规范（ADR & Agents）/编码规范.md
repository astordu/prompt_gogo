- ADR 文件统一以四位数字序号加连字符命名（如 0001-xxx.md），并按时间顺序递增编号。
- 每个 ADR 固定包含 Considered Options 与 Consequences 两个章节，前者列出被否决的方案及其原因，后者说明实施后的不可逆影响与回退策略。
- Agent 文档中所有外部工具调用均以具体命令行示例呈现（如 gh issue create --title "..." --body "..."），而非抽象描述。
- 分诊角色通过表格在 skills 标签与实际仓库标签之间建立一一映射，确保 Agent 输出与仓库约定保持一致。