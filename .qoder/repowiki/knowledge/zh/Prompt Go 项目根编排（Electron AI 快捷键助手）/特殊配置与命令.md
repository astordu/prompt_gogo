开发：npm run dev（Electron --dev）、npm start（生产模式）；测试：npm test（node:test）、npm run coverage（c8 HTML+text 报告）；校验：npm run verify（lint + typecheck + 60% 行覆盖率门槛）；打包：npm run dist（electron-builder --mac dmg）。macOS 首次安装需执行 xattr -cr /Applications/Prompt\ Go.app 清除隔离标记并授予辅助功能权限。